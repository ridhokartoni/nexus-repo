# Local .terraform directories
**/.terraform/*

# .tfstate files
*.terraform.lock.hcl
*.tfstate
*.tfstate.backup

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data, such as
# private keys. If you have committed sensitive data, you should rotate those
# credentials.
*.tfvars

# Ignore override files as they are usually used for local testing or sensitive data.
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# CLI configuration files
.terraformrc
terraform.rc

# Mac and Windows system files
.DS_Store
Thumbs.db

